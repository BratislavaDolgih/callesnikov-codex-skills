interface:
  display_name: "Callesnikov QRify"
  short_description: "Generate styled QR codes locally"
  default_prompt: "Use $callesnikov-qrify to generate a styled QR code in the current directory."
