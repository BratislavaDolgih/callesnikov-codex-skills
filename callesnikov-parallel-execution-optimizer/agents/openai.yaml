interface:
  display_name: "Callesnikov Parallel Execution Optimizer"
  short_description: "Run safe dependency-aware parallel Codex work"
  default_prompt: "Use $callesnikov-parallel-execution-optimizer to split this task into safe parallel lanes and verify the integrated result."

policy:
  allow_implicit_invocation: true
