interface:
  display_name: "Kolesnikov Obsidian Refactorer"
  short_description: "Careful Kolesnikov Obsidian vault refactors"
  default_prompt: "Use $kolesnikov-obsidian-refactorer to inspect and carefully refactor a local Obsidian vault while preserving structure, links, embeds, frontmatter, encoding, and Markdown consistency."

policy:
  allow_implicit_invocation: true
