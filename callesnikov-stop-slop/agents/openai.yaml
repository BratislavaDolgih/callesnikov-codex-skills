interface:
  display_name: "Callesnikov Stop Slop"
  short_description: "Remove AI tells from prose"
  default_prompt: "Use $callesnikov-stop-slop to rewrite this draft so it sounds direct, specific, and human."
policy:
  allow_implicit_invocation: true
